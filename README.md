## Flake configurations

This repository contains the Nix flake configurations for my NixOS laptop.

To build and switch to them, clone this repository and in `nix-shell` run `sudo nixos-rebuild switch`.